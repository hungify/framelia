---
"@framelia/contracts": minor
"@framelia/verify": minor
"framelia": minor
---

Complete the npm-consumer CLI workflow from idempotent setup through authored contracts and explicit run inspection.

- Add non-destructive init planning, dry-run output, explicit-root environment loading, one-object authored contract creation from Figma design URLs, immutable content-addressed baseline publication, CAS-protected force replacement, exact baseline refresh, and tolerant authored/binding listing.
- Share safe local Playwright collection between `contract list` and `check`, and project durable runs into deep versioned check, report, open, and done-gate identities with per-case attempts and portable evidence references.
- Emit one readiness record after open/dashboard starts listening, keep operational logs on stderr, and return structured finite-operation outcomes for route-level missing input.
