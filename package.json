{
  "name": "framelia-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Visual verification monorepo for Figma-to-web and web-to-web workflows.",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @framelia/dashboard-server build",
    "dev:dashboard": "pnpm --filter @framelia/dashboard dev",
    "framelia": "pnpm --filter framelia cli",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @framelia/verify test && pnpm --filter @framelia/dashboard-server test && pnpm --filter @framelia/playwright test && pnpm --filter framelia test && pnpm --filter @framelia/dashboard test",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "prepare": "simple-git-hooks",
    "validate": "pnpm run typecheck && pnpm run test && pnpm run lint && pnpm run fmt:check",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "lint-staged": "^17.3.0",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "simple-git-hooks": "^2.13.1",
    "typescript": "6.0.3"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.18.0"
}
